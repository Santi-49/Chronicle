# gateway-client

Client for the FastAPI control plane: login/auth (JWT), usage logs, stats,
and the hosted AI-inference gateway. Types are imported from
`packages/contracts/api/generated` — never hand-written.
