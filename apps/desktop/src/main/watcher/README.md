# watcher

Folder watching: user-selected directories, save detection with debounce,
temp-file/autosave ignore rules. A changed content hash creates a new version.
Rules are defined at the interfaces milestone (docs/challenge/CONSTRAINTS.md).
